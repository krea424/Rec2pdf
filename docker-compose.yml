services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./rec2pdf-backend:/app/rec2pdf-backend
    env_file:
      - ./rec2pdf-backend/.env
    
    # === AGGIUNGI QUESTA SEZIONE ===
    environment:
      - WORKER_SECRET=gdhysjkd67brk45bd68sbd43hdk
    # ===============================
      
    command: npm run dev