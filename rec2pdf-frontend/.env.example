# ===============================================
# === ESEMPIO DI CONFIGURAZIONE PER IL FRONTEND =
# ===============================================
# Questo file serve come modello. Copialo in .env.local e inserisci le tue chiavi.

# -- Credenziali PUBBLICHE per Supabase --
VITE_SUPABASE_URL="INSERISCI_IL_TUO_URL_SUPABASE"
VITE_SUPABASE_ANON_KEY="INSERISCI_LA_TUA_CHIAVE_ANON_PUBBLICA_SUPABASE"
VITE_SUPABASE_STORAGE_LOGOS_BUCKET="logos"

# -- URL del backend a cui il frontend si connette --
# Per lo sviluppo locale, di solito è "http://localhost:7788"
VITE_API_BASE_URL="http://localhost:7788"

# -- Flag per le modalità UI (opzionale) --
VITE_DEFAULT_MODE_FLAGS=MODE_BASE,MODE_ADVANCED

# -- Flag per bypassare l'autenticazione in locale (opzionale) --
VITE_BYPASS_AUTH=false
