# ===============================================
# === ESEMPIO DI CONFIGURAZIONE PER IL BACKEND ==
# ===============================================
# Questo file serve come modello. Copialo in .env e inserisci le tue chiavi.

SUPABASE_URL=https://dvb...
SUPABASE_SERVICE_KEY=eyJ...
# Chiave service role (uso interno backend)
SUPABASE_SERVICE_ROLE=eyJ...
SUPABASE_STORAGE_LOGOS_BUCKET=logos
HUGGING_FACE_TOKEN=hf_GhH......
AI_TEXT_PROVIDER=gemini-pro
AI_TEXT_PROVIDER_FALLBACK=gemini
AI_EMBEDDING_PROVIDER=gemini

# Backend base URL per gli script di valutazione
BACKEND_URL=http://localhost:3001

# === Configurazione Evaluation (RAG baseline score) ===
EVAL_LLM_PROVIDER=gemini   # gemini | openai | mock
EVAL_MODEL_NAME=gemini-2.5-flash  # es. gemini-2.5-flash | gpt-4o-mini
EVAL_CONCURRENCY=2
EVAL_TIMEOUT_MS=20000
EVAL_MAX_CONTEXT_CHARS=2000

# API Keys per il grader LLM (imposta solo quella in uso)
GOOGLE_API_KEY=your_gemini_api_key
OPENAI_API_KEY=your_openai_api_key
